const UserMenu = () => {
  const name = "";
  const dispatch = "";
  return (
    <div>
      <p>Welcome to Game {name} </p>
      <button type="button" onClick={() => dispatch()}>
        Logout
      </button>
    </div>
  );
};

export default UserMenu;
